var basic = `a basic literal`;

var multiline = `a
multiline
literal`;

// interpolation
var name = 'Alice';
var greeting = `Hello ${name}!`;

// expressions, too
var calculation = `2+2=${2 + 2}`;

/*
// tagged template literals
var triumph = 'triumph';
function process() {
  return arguments[0];
}
var hugeSuccess = process `this was a ${triumph}`;
*/
